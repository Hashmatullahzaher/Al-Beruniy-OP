NEXT_PUBLIC_APP_NAME=AL-BERUNIY Operating System
NEXT_PUBLIC_APP_ENV=development
# Optional build-system override. The app otherwise reads the current Git HEAD.
# GIT_SHA=
# Separate logins inheriting only their matching restricted runtime roles.
ABOS_TREASURY_DATABASE_URL=
ABOS_FINANCE_DATABASE_URL=
